package solution42;

import java.util.ArrayDeque;

class Solution {
    public int trap(int[] height) {
        if(0 <= height.length && height.length <= 2)
            return 0;
        ArrayDeque<Integer> deque = new ArrayDeque<>();
        int result = 0;
        
    }
}
